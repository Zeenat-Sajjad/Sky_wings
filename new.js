//function toggleNavbar(navbarId) {
  //  var navbar = document.getElementById(navbarId);
    //var mainNavbar = document.getElementById('mainNavbar');

    // Hide all sub-navbars
    //var subNavbars = document.querySelectorAll('.sub-navbar');
    //subNavbars.forEach(function (item) {
     //   item.classList.remove('show');
    //});

    // Show the selected sub-navbar
   // navbar.classList.toggle('show');

    // Adjust the z-index of the main navbar to ensure it stays on top
 //   mainNavbar.style.zIndex = (navbar.classList.contains('show')) ? '0' : '1';
//}